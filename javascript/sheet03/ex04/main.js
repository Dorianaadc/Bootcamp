/* 4. Función con varias líneas de código*/

// Función normal
const multiplica = function(a, b) {
    const resultado = a * b;
    return resultado;
};


// Función de flecha
const multiplica2 = (a, b) => a * b;
